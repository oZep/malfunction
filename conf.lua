function love.conf(w)
    w.window.title = "SegSheep Fault"         -- The window title (string)
    w.window.icon = "gfx/character.png"                 -- Filepath to an image to use as the window's icon (string)
    --% Debug %--
    w.console               =       true        -- preferencial for me --
end